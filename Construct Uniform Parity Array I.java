class Solution {
    public boolean uniformArray(int[] nums1) {
        // It is always mathematically possible to make all elements odd 
        // if there is a mix of parities, or keep them the same if they aren't.
        return true;
    }
}
